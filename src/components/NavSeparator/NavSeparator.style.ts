import { StyleSheet } from "react-native";

const NavSeparatorStyle = StyleSheet.create({
  separator: {
    borderWidth: 0.5,
    borderColor: "#DDD",
    marginBottom: 12,
  },
});

export default NavSeparatorStyle;
