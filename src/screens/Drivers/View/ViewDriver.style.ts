import { StyleSheet } from "react-native";

const ViewDriversStyle = StyleSheet.create({
  statsContainer: {
    paddingHorizontal: 20,
    paddingVertical: 15,
  },
  row: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
  },
  bold: {
    fontWeight: "bold",
  },
  marginTop: {
    marginTop: 20,
  },
});

export default ViewDriversStyle;
