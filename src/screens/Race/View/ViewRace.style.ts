import { StyleSheet } from "react-native";

const ViewRaceStyle = StyleSheet.create({
  raceTrack: {
    fontSize: 16,
    marginHorizontal: 72,
    marginBottom: 12,
  },
});

export default ViewRaceStyle;
