import { StyleSheet } from "react-native";

const RandomMapStyle = StyleSheet.create({
  container: {
    paddingHorizontal: 50,
    display: "flex",
    alignItems: "center",
    height: "100%",
    justifyContent: "center",
  },
  text: {
    marginTop: 16,
    textTransform: "uppercase",
    fontSize: 18,
    textAlign: "center",
    marginBottom: 24,
  },
});

export default RandomMapStyle;
